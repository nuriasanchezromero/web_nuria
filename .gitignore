# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables (todas las variantes: .env.local, .env.production.local...)
.env*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local secrets for wrangler dev (.dev.vars y .dev.vars.<entorno>)
.dev.vars*
!.dev.vars.example

# unrelated media project living in this folder
Sinuosa_Klimt_CapCut_v3/

# seeded design canvas output (regenerated from design/*.dc.html)
design/*.html
!design/*.dc.html

# wrangler local state
.wrangler/

# worktrees de agentes
.claude/worktrees/

# nota de estado local de las herramientas de agentes (no es del proyecto)
/TLDR.md

# notas privadas sobre Núria para redactar la web: el repositorio es público,
# así que nunca deben subirse (tampoco con git add -A)
/contexto-sobre-nuria.md
